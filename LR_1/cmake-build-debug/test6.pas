{快速排序}
program test9(input, output);
  var num : Integer;
  t : Integer;
  procedure initial(var a: Integer);
    begin
      num := 20;
    end;{end of intital}

  procedure print(var a: Integer);
    var i :Integer;
      begin
        for i := 0 to 0 do
          WriteLn(num);
      end;{end of print}

  begin
    initial(1);
    print(1);
  end.{end of program}
